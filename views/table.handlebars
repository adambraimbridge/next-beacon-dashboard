<section class="feature__container">
    <h1 class="feature__name">{{ title }}</h1>
	<table>
		{{#each data}}
		<tr>
			<td>{{ key }}</td>
			<td>{{ result }}</td>
		</tr>
		{{/each}}
	</table>
</section>
